<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- GPS / location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Keep tracking trip/stage when backgrounded / screen off. This is done by
         the FOREGROUND SERVICE below, NOT by ACCESS_BACKGROUND_LOCATION.

         That permission was declared here and never once requested: nothing in
         lib/ calls Permission.locationAlways, and ensurePermission() only calls
         Geolocator.requestPermission(), which asks for FOREGROUND location. A
         declared-but-never-granted permission confers no capability, so removing
         it cannot change what the app can do — it only removes the obligation to
         justify one of the most heavily scrutinised permissions on both stores.
         Re-add it ONLY alongside a real runtime request and its own rationale.
         See docs/SECURITY-REVIEW.md S1. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Android 13+ (API 33): required to post the foreground-service notification.
         Without this the geolocator foreground service can fail to start and the
         position stream silently dies (no speed / no trip distance). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Map tiles + GPX share -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Device must have GPS hardware -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true"/>

    <application
        android:label="iRallyMeter"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
